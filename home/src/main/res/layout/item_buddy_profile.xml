<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View"/>

        <import type="com.nocapstone.home.dto.BuddyData" />

        <variable
            name="buddyData"
            type="BuddyData" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">


        <androidx.cardview.widget.CardView
            android:id="@+id/buddy_profile"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_margin="8dp"
            android:elevation="16dp"
            app:cardBackgroundColor="@{buddyData.select ? @color/butter : @color/white}"
            app:cardCornerRadius="50dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                photoUrl="@{buddyData.buddyProfileUrl}"
                android:layout_width="45dp"
                android:layout_height="45dp"
                android:layout_gravity="center"
                tools:src="@drawable/img_dog" />

        </androidx.cardview.widget.CardView>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginVertical="8dp"
            android:fontFamily="@font/cafe24_ssurround_air"
            android:text="@{buddyData.buddyName}"
            android:textColor="@color/black"
            app:layout_constraintEnd_toEndOf="@id/buddy_profile"
            app:layout_constraintStart_toStartOf="@id/buddy_profile"
            app:layout_constraintTop_toBottomOf="@id/buddy_profile"
            tools:text="주주" />


    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
